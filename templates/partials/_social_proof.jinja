<p class="text-center text-sm text-gray-500">
  <strong class="text-gray-700 dark:text-gray-300">{{ files_processed|default(0) }}</strong> files processed
  · trusted by <strong class="text-gray-700 dark:text-gray-300">{{ user_count|default(0) }}</strong>+ users
</p>
